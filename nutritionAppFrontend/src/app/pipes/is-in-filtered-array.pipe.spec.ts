import { IsInFilteredArrayPipe } from './is-in-filtered-array.pipe';

describe('IsInFilteredArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new IsInFilteredArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
