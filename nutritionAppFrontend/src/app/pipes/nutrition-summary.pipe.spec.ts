import { NutritionSummaryPipe } from './nutrition-summary.pipe';

describe('NutritionSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new NutritionSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
