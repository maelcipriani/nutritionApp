import { RecipeTypePipe } from './recipe-type.pipe';

describe('RecipeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
