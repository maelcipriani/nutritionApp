import { CalculateItemPricePipe } from './calculate-item-price.pipe';

describe('CalculateItemPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateItemPricePipe();
    expect(pipe).toBeTruthy();
  });
});
